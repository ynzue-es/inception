services:
  mariadb:
    secrets:
        - db_root_password
        - db_password
  
  nginx:
    ports:
      - "443:443"

  wordpress:


secrets:
  db_root_password:
    file: ./secrets/db_root_password.txt
  db_password:
    file: ./secrets/db_password.txt